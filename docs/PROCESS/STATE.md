# STATE

<!-- Текущий срез работ. Обновляется при каждом значимом изменении состояния. -->

## Сейчас в работе
**Bootstrap-сессия (Слой 0).** Собран пакет проектной документации, семантическая память и полный
контур API-тестов (POM, Python + pytest + httpx). Код продукта НЕ написан (тип задания B).

## Статус по областям
- Документация: ✅ собрана (PRODUCT, ARCHITECTURE+ADR×8, PROCESS, OPERATIONS, QA).
- Семантическая память: ✅ засеяна (`memory/` + индекс).
- Тест-контур: ✅ написан; статус прогона — **TDD red** (нет реализации/запущенного API). Контур
  обязан коллектиться (`pytest --collect-only`).
- CI: ✅ workflow `tests.yml` (collect-only гейт + опциональный прогон).
- Git/GitHub: см. CHRONICLE — инициализация, коммиты, push.
- Реализация продукта: ⛔ не начата (ожидается апрув).

## Блокеры / ожидания
- Ожидается **апрув на старт основной разработки**.
- Открытые вопросы Q-1..Q-8 (см. [[REQUIREMENTS]] и [[CHRONICLE]]) — требуют решения заказчика;
  приняты безопасные дефолты-`[ASSUMPTION]`.

## Next steps (после апрува)
1. Слайс 1 — каркас FastAPI-приложения (см. [[ROADMAP]]).
2. Слайс 2 — Employees.
3. Прогонять контур из [[TEST_PLAN]] по мере реализации (red → green).

---

*Graph: [[AGENT_CONTEXT]] · [[ROADMAP]] · [[CHRONICLE]] · [[TEST_PLAN]]*
