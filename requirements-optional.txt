# Optional production backends. The app runs without these (guarded imports + in-process fallbacks):
# - redis: distributed rate limiting (REDIS_URL) and the arq broker
# - arq: background processing worker (app/worker.py)
# - sentry-sdk: error reporting on production (SENTRY_DSN)
redis>=5.0,<6
arq>=0.26,<1
sentry-sdk>=2.0,<3
